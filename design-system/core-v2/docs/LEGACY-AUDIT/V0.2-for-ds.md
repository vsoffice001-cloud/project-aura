# V0.2 Audit — `projects/V0.2 -for design system/`

**TL;DR:** Dense Qatar-herbs PDP w/ 24 sections + 65 UI atoms. Token system = KP 2.0 (`--brand-red/--purple-*/--black-*`) — entirely misaligned w/ core-v2 OG ramp tokens. **KEEP:** data-viz primitives (MindMap, RegionalComparison, SegmentationCard, StatCardGroup, TOC sidebar, MarketDataTable, TimelineCard). **SKIP:** all Buttons, FloatingCTA, hero CTA stack, ResearchMethodology stepper. Type pairing strong. Color/spacing values mostly correct intent but wrong token names.

---

## 1. Section Inventory

| File:line | Section | WHAT | Verdict | WHY |
|---|---|---|---|---|
| `HeroSection.tsx:51` | Hero | Cinematic-dark video bg + glass card stat grid + audio player + 2 CTAs | 🔴 PARTIAL | KEEP glass card grid pattern; SKIP CTAs/Buttons (user rule) |
| `Header.tsx:48` | Sticky navbar | scroll-spy bg-flip white/dark + 4 dropdowns + breadcrumb + progress bar | 🔴 SKIP | Use dummy header from topnav-v32 per MEMORY |
| `MarketOverview.tsx:14` | Chapter 1 intro + 4 stats + outlook card + 4 timeline cards | ✅ KEEP | Small-stats + text-pairing canonical |
| `ScopeOfReport.tsx:240` | Mind-map preview w/ click→modal expand | ✅ KEEP | Taxonomy + MindMap + hidden modal layer |
| `MarketAnalysis.tsx:269` | Chapter 3 · 3 Highcharts (area · column · line) + 2 insight cards | ✅ KEEP | charts/graphs |
| `MarketDataTable.tsx:202` | Sortable 12-row × 7-col table w/ progress bar inline + period badge | ✅ KEEP | data tables canonical |
| `SegmentationSection.tsx:62` | 7 segmentation cards (2/3/2 grid) + key takeaways gradient card | ✅ KEEP | SegmentationCard canonical |
| `RegionalComparison.tsx:132` | bar chart + comparison table w/ blurred paywall rows + 3 insight cards | ✅ KEEP | charts + data tables + hidden-content layer |
| `GrowthDriversChallenges.tsx:5` | Chapter 7 · 3 desktop stats + 3 IconCards w/ nested h4+bullet lists | ✅ KEEP | dense-data composition |
| `CompetitiveLandscape.tsx` (537L) | Chapter 8 player profile blocks (not fully read · use as ref) | ✅ KEEP | charts/bars + comparison |
| `TargetAudience.tsx:61` | 8 StakeholderCard grid (2col) + benefits aside card w/ stats + CTA | ✅ KEEP grid; 🔴 SKIP CTA |
| `ResearchMethodology.tsx:53` | 3-step stepper Button row + 3 MethodologyCards | 🔴 SKIP stepper Buttons; ✅ KEEP card layout |
| `FAQSection.tsx:52` | 8-item shadcn Accordion | ✅ KEEP | hidden-content layer (accordion) |
| `RelatedReports.tsx` (251L) | Listing | ✅ KEEP card grid · 🔴 SKIP CTA |
| `Footer.tsx` (264L) | Footer | 🔴 SKIP | Use dummy footer per MEMORY |
| `FinalCTA.tsx:1` | Final CTA section (42L) | 🔴 SKIP | CTA rule |
| `FloatingCTA.tsx:46` | Bottom-rising sticky banner after hero past, before final-CTA visible | 🔴 SKIP | User explicit |
| `TableOfContentsSection.tsx` (572L) | Inline TOC section variant | ✅ KEEP layout |
| `TableOfContentsSidebar.tsx:54` | sticky-left TOC w/ collapse · scroll-spy · completed/active/upcoming states | ✅ KEEP | SideTOC canonical |
| `MindMap.tsx:12` | d3 hierarchy tree · 3-depth collapse · zoom/pan · 360px/depth · 36px pill nodes | ✅ KEEP |
| `MindMapModal.tsx:12` | 95vw×90vh modal · ESC close · body lock | ✅ KEEP |
| `AudioPlayer.tsx` (1119L) | Audio summary player on hero | ⚠️ NEUTRAL · not in user keep/skip explicit |
| `SectionHeader.tsx` | h2 wrapper | ✅ KEEP small-pattern |
| `InlineStats.tsx:8` | desktop-inline + mobile-grid stat pair · superseded by `StatCardGroup` | ✅ KEEP pattern (use group atom) |

---

## 2. Button Inventory — 🔴 ALL OUTDATED (catalogued for completeness only)

`ui/button.tsx:7` — CVA buttonVariants. 8 variants × 4 sizes.

| Variant | Spec | Verdict |
|---|---|---|
| `default` | `bg-primary text-primary-foreground hover:bg-primary/90` | 🔴 |
| `destructive` | shadcn standard destructive | 🔴 |
| `outline` | `border-[var(--black-200)] bg-white hover:bg-[var(--black-50)] hover:text-[var(--brand-red)]` | 🔴 |
| `secondary` | `border-[var(--black-300)] bg-transparent hover:bg-[var(--black-50)] rounded-[var(--radius-sm)]` | 🔴 |
| `cta` | `bg-gradient-to-br from-[var(--brand-red)] to-[var(--red-500)] shadow-[var(--shadow-brand-red)]` | 🔴 (gradient violates R1.2 brand-red solid only) |
| `ctaBlack` | `bg-[var(--black)] hover:bg-[var(--black-900)] rounded-[var(--radius-sm)]` | 🔴 |
| `glass` | `bg-[var(--glass-bg)] backdrop-blur-sm border-[var(--glass-border)]` | 🔴 (hero-only · keep glass tokens not Button) |
| `ghost` · `link` | shadcn defaults | 🔴 |

Sizes: `default/lg` both `h-12 rounded-md px-6` (drift · should be 48/56) · `sm` `h-8` (drift · OG 40) · `icon size-9`.

Usage sites: `HeroSection:127,134,208` · `RegionalComparison:227` · `FloatingCTA:63,70` · `ResearchMethodology:86` · `TargetAudience:130` · `Footer` · `FinalCTA`.

---

## 3. Arrow + Directional UX — 🔴 SKIP (report-store wins)

| Component | File:line | Pattern |
|---|---|---|
| Hero CTA arrow | `HeroSection:139,213` | `<ArrowRight className="h-5 w-5"/>` inside Button after label |
| Scroll indicator orb | `HeroSection:222` | `<div className="w-6 h-10 rounded-full border-2 border-white/20"><div className="w-1 h-2 bg-white/40 rounded-full"/>` · floating-y sine |
| Caret sortable col | `MarketDataTable:90` | `<CaretUpDown weight="regular" className="h-4 w-4 ml-1 opacity-50 text-[var(--purple-500)]"/>` |
| ChevronRight stepper | `ResearchMethodology:106` | between Button steps · `text-[var(--black-300)] mx-2` |
| ChevronRight list-bullet | `MethodologyCard:117` | `text-[var(--purple-500)]` before each item |
| Sidebar collapse chevron | `TableOfContentsSidebar:75` | `<ChevronLeft>` rotates 180 collapsed · 8×8 white pill bg · `-right-4` outside edge |
| Maximize2 mindmap hover | `ScopeOfReport:297` | corner gradient-to-t hover overlay w/ Maximize2 icon |
| Header ArrowUpRight | `Header:268` | inside "Book discovery call" Button |
| Footer · RelatedReports | similar inline arrow CTA pattern | 🔴 |

All red-tinted/purple-tinted icon usage misaligns w/ new icon-color taxonomy (`--icon-content/utility/brand/on-dark`).

---

## 4. Type Pairing Inventory — ✅ KEEP

**Eyebrow → H2 → lead (Section Header Block):**
```jsx
<OverheadText>CHAPTER 1 - INDUSTRY ANALYSIS</OverheadText>      // text-sm brand-red bold uppercase tracking-widest
<SectionHeader>Qatar Fresh Herbs Market Overview</SectionHeader> // font-display text-3xl tracking-tight text-[#171717]
<BodyText spacing="first">...</BodyText>                          // text-base leading-relaxed text-black-500 mt-6
```
Files: `MarketOverview:31-48`, `MarketDataTable:206-216`, `SegmentationSection:66-75`, `ScopeOfReport:267-277`.

**Stat-card pair (StatCard inline):**
```jsx
<p className="text-2xl lg:text-3xl font-bold tracking-tight text-[#171717]">10%</p>
<p className="text-sm mt-1.5 text-[#737373]">Health Market Growth</p>
```
Files: `GrowthDriversChallenges:27-28`, `InlineStats:18-19`, `TimelineCard:42-46`.

**Label→value (Hero glass card):**
```jsx
<div className="flex items-center gap-2 text-[var(--glass-text-muted)] text-xs uppercase tracking-wider">
  <Calendar className="h-4 w-4" /><span>Base Year</span>
</div>
<p className="text-2xl font-normal text-[var(--glass-text)]">2024</p>
```
File: `HeroSection:163-168`.

**Stakeholder pair:**
```jsx
<h4 className="text-base font-bold text-foreground mb-1">{title}</h4>
<p className="text-sm leading-relaxed text-[var(--black-500)]">{description}</p>
```
File: `stakeholder-card.tsx:70-75`.

**Card title + description + stat-strip (TextCard):**
```jsx
<h3>{title}</h3>                                          // inherits h3 default
<p className="text-base leading-relaxed text-[var(--black-600)]">{paragraph}</p>
<p className="text-[26px] font-bold text-primary|black-900">{stat.value}</p>
<p className="text-[14px] text-[var(--black-600)]">{stat.label}</p>
```
File: `text-card.tsx:117-156`.

**Segmentation row pair:**
```jsx
<span className="text-sm text-[var(--black-900)]">{item.name}</span>
<span className="text-xs text-[var(--black-500)]">({item.cagr})</span>
<span className="text-sm font-bold text-[var(--black-900)] ml-2">{item.share}%</span>
```
File: `segmentation-card.tsx:126-143`.

**Item h4 + body + ul-bullet (GrowthDrivers):**
```jsx
<h4 className="text-[#171717] mb-2">Increasing Health Consciousness</h4>
<p className="text-[16px] leading-relaxed mb-3 text-[#737373]">...</p>
<li className="flex items-start gap-2"><TrendingUp .../><span>...</span></li>  // text-sm
```
File: `GrowthDriversChallenges:76-89`.

---

## 5. Type Scale + Weight + Tracking + Leading — ⚠️ misaligned tokens

| Value | Where | core-v2 match | Status |
|---|---|---|---|
| `text-[13px]` | overhead labels `GrowthDrivers:11`, `MarketAnalysis:274` | none (closest `--text-xs` 12.8px) | ⚠️ drift |
| `text-sm` (14px theme · 0.8125rem) | body/labels everywhere | core-v2 `--text-sm = 1rem` (16px) | 🔴 NAME COLLISION |
| `text-base` (14px theme) | body | core-v2 `--text-base = 1.25rem` (20px) | 🔴 NAME COLLISION |
| `text-[16px]` inline | desc body `GrowthDrivers:18`, `IconCard:116` | core-v2 `--text-sm = 16px` | ⚠️ |
| `text-lg` (18px theme) | card titles `SegmentationCard:109` | core-v2 `--text-md = 18px` mismatch | ⚠️ |
| `text-xl` (20px theme) | hero `h3 Report Details:156` | core-v2 `--text-md = 18px` | ⚠️ |
| `text-2xl` (24px theme) | mobile stat `GrowthDrivers:27` · modal h2 `MindMapModal:53` | core-v2 `--text-xl` 31px ≠ | ⚠️ |
| `text-[26px]` inline | card stats `TextCard:147`, `TimelineCard:46` | no direct token | 🔴 hardcoded |
| `text-3xl` (30px theme) | unused in samples | n/a | n/a |
| `text-4xl` (32px theme · `--text-4xl 2rem`) | hero h1 `HeroSection:112` | core-v2 `--text-3xl = 48.8px` ≠ | ⚠️ |
| `text-[48px]` inline | section h2 `RegionalComparison:139`, `GrowthDrivers:14` | core-v2 `--text-2xl 39px` / `--text-3xl 48.8px` ✅ ish | ⚠️ |
| Weight `font-normal 400` | hero h1, h3 glass | core-v2 `--font-weight-normal 400` | ✅ |
| Weight `font-medium 500` | nav text | ✅ | ✅ |
| Weight `font-bold 700` | StatCard value, OverheadText, badge | core-v2 ships 400+500 only | 🔴 OG strips 700 |
| Tracking `tracking-tight` (-0.025em) | hero h1 | core-v2 `--tracking-tight: -0.01em` | ⚠️ drift |
| Tracking `tracking-widest` (0.1em) | OverheadText, breadcrumb | core-v2 `--tracking-widest: 0.1em` | ✅ |
| Tracking `tracking-wider` (0.05em) | glass label uppercase | core-v2 ✅ | ✅ |
| Tracking `tracking-[1.62px]` | nav dropdown labels `Header:91` | none · hardcoded | 🔴 |
| Tracking `tracking-[0.62px]` | nav items `Header:101` | none | 🔴 |
| Leading `leading-tight` (1.25) | hero h1 `HeroSection:112` | core-v2 `--leading-tight: 1.1` | ⚠️ |
| Leading `leading-relaxed` (1.625) | body | core-v2 `--leading-relaxed: 1.6` | ✅ ish |
| Leading `leading-[1.1]` inline | hero h1 | core-v2 ✅ | ✅ |
| Font `font-display` (Noto Serif) | hero h1, section h2 | core-v2 `--font-serif` ✅ | ✅ |
| Font `font-sans` (DM Sans) | body, all UI text | core-v2 `--font-sans` ✅ | ✅ |

**Verdict:** Sizing intent good but `text-sm/base/lg/xl/2xl` name collision will silently break porting. Hardcoded `text-[Npx]` everywhere — must port to OG short-scale.

---

## 6. Color / Palette Usage

**Brand red (`#b01f24` · `--brand-red` · `--primary-500`):** OverheadText `var(--brand-red)`, Header CTA `bg-[#B01F24]`, Footer, FinalCTA, breadcrumb progress `bg-[#b01f24]`, hero CTA gradient. ✅ matches core-v2 `--brand-red`.

**Data purple (`#7f5fe3` · `--purple-500`):** SegmentationCard progress bar, MarketAnalysis chart series, MethodologyCard chevron icons, AnalysisCard number text `#7f5fe3`, IconCard default iconColor. ⚠️ core-v2 canonical purple is `#806ce0` (`--purple-600`) — close but distinct. `--purple-100` (`#eff1fe`) used for icon bg ⚠️ core-v2 `#efedfd`.

**Periwinkle (deprecated · aliased to purple):** `iconBg='periwinkle-50' #eff1fe`, `iconColor='#6D52D9'` StatCard atom · stat-card uses `bg-warm-200` literal class on line 151 (broken — undefined). 🔴

**Black/grey ramp:**
- `#171717` (`--black-900` · text primary) ✅ core-v2 ramp ok
- `#737373` (`--black-500` · secondary) ✅
- `#525252` (`--black-600` · stroke level 2 mindmap) ✅
- `#404040` (`--black-700`) ✅
- `#d4d4d4` (`--black-300`) ✅
- `#e5e5e5` (`--black-200` · borders) ✅
- `#f5f5f5` (`--black-100` · card bg accent) ✅
- `#fafafa` (`--black-50` · section bg even) ✅ core-v2 alias `--neutral-50`
- `#fcfcfc` (`--black-25`) ⚠️ no core-v2 equiv
- `#656565` (Header dropdown labels) 🔴 hardcoded · not in core-v2 ramp

**Glass tokens (Hero-only):** `rgba(255,255,255,0.08)` bg · `0.15` border · `0.05` glow · `0.95` text · `0.5` muted · `rgba(176,31,36,0.8)` accent · `0.12` hover. 🔴 entirely absent from core-v2 — must port if hero pattern kept.

**Semantic:**
- `var(--green-600) #16a34a` Growth driver icons → core-v2 `#059669` 🔴 different green
- `var(--red-600) #dc2626` Challenge icons → core-v2 self-contained `#dc2626` ✅
- `var(--amber-400) #fbbf24` Opportunity icons → core-v2 ✅
- `var(--green-100/700)` table period badge → core-v2 ✅ ish

**Highcharts inline colors:** `var(--purple-300/400/500/600/700)` in series `RegionalComparison:121-127`. Mapping to core-v2 ramp possible.

**Hardcoded misc:** `#f4f2fc` AnalysisCard icon bg (line 69) 🔴, `#fafbfe`-ish gradient backgrounds in TableOfContentsSidebar `bg-white/80` ✅, `rgba(243,244,255,0.5)→rgba(250,250,250,0.3)` gradient takeaways card (line 146) 🔴 violates gradient ban R1.2.

---

## 7. Spacing Inventory

**Section vertical:** `py-24 lg:py-32` (96/128px) — EVERYWHERE (`MarketOverview:18`, `Scope:254`, `MarketAnalysis:270`, `GrowthDrivers:6`, `FAQ:53`, `Methodology:54`, `TargetAudience:62`). Some variants `py-20 lg:py-24` (`SegmentationSection:63`, `MarketDataTable:204`). Core-v2 `--section-py-desktop = 5rem` (80px) — **drift: V0.2 sections taller**.

**Section horizontal:** `px-[84.375px] lg:px-[112.5px]` — hardcoded · matches theme.css `--section-padding-x-mobile/desktop`. 🔴 odd px values, not in core-v2 (`--padding-desktop 2rem` 32px).

**Container:** `max-w-7xl mx-auto` (1280px) everywhere · MarketOverview uses `max-w-[var(--content-max-width)]` (undefined token).

**Section-header block:** `mb-16` (64px) after eyebrow+h2+lead block → before content. Core-v2 `--section-header-mb: 3rem` (48px) — drift.

**Eyebrow → h2:** `mb-4` (16px) `OverheadText` parent div, or `mb-2` `MarketOverview:32`. Core-v2 `--pair-label-heading: 0.75rem` (12px) — drift.

**H2 → body:** `pt-3` `RegionalComparison:142`, `paddingTop:10px` `GrowthDrivers:18`, `mb-6` `FAQ:73`. Inconsistent.

**Stats divider line:** `pt-10 mt-10 border-t border-[var(--black-200)]` `RegionalComparison:146`, `SegmentationSection:77`, `GrowthDrivers:22`.

**Grid gaps:**
- Card grids 3-col: `gap-6 lg:gap-8` (24/32px)
- Stat grids: `gap-5 mt-8` (20px gap, 32px from prev)
- SegmentationCard grid: `gap-6 mb-6` (24px)
- Inline stats: `gap-10 lg:gap-14` (40/56px) `GrowthDrivers:24`

**Card padding:** `p-4` (16px) IconCard/SegCard/AnalysisCard/StatCard icon-left · `p-5` StakeholderCard · `p-8` MethodologyCard/Hero glass card/TargetAudience aside · `p-6` TextCard header · `px-6 pb-6` text-card content. Core-v2 `--card-padding-md: 1rem` ✅ matches `p-4`.

**Card internal vertical:** `space-y-6` between subsection blocks GrowthDrivers · `space-y-4` segmentation items · `space-y-3` methodology items · `space-y-2` glass-card label-value pairs.

**Stat-card internal:** `mb-1` between label and value · `mb-2/3` between value and subtitle.

**Sidebar TOC:** `top-[72px]` sticky · `h-[calc(100vh-72px)]` · `py-4` · `px-2/4` nav · `space-y-0.5` items · 255px open / 80px collapsed width.

---

## 8. Composition Recipes (10 captured)

**1. Chapter section header (canonical):**
```jsx
<section id="x" className="py-24 lg:py-32 bg-[white|black-50]">
  <div className="max-w-7xl mx-auto px-[84.375px] lg:px-[112.5px]">
    <div className="mb-16">
      <div className="mb-4"><OverheadText>CHAPTER N - LABEL</OverheadText></div>
      <SectionHeader>Title<span className="block">Wrap</span></SectionHeader>
      <BodyText spacing="first" className="max-w-3xl">...</BodyText>
      <div className="pt-10 mt-10 border-t border-[var(--black-200)]">
        <StatCardGroup stats={[...]} />
      </div>
    </div>
    {/* content blocks */}
  </div>
</section>
```

**2. MindMap embed + modal launcher (`ScopeOfReport:281-303`):**
```jsx
<div className="flex justify-center">
  <div onClick={() => setIsModalOpen(true)} className="w-full overflow-hidden">
    <div className="border border-[var(--black-200)] bg-white rounded-[var(--radius-md)] hover:shadow-[var(--shadow-brand-periwinkle)] overflow-hidden">
      <div className="h-[600px] relative group">
        <MindMap data={data} interactionMode="preview" searchTerm="" />
        <div className="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 flex items-end justify-center pb-8">
          <div className="flex items-center gap-2 text-white"><Maximize2/><span>Click to Explore</span></div>
        </div>
      </div>
    </div>
  </div>
</div>
<MindMapModal isOpen={...} onClose={...} data={...} />
```

**3. RegionalComparison · chart+table side-by-side w/ paywall:**
2-col grid (`lg:grid-cols-2 gap-6 lg:gap-8`) wrapping `<TextCard>` containing `<Chart>` or `<Table>`. Paywall rows = `blur-sm pointer-events-none` siblings + absolute centered `<Button variant="cta">` overlay.

**4. SegmentationSection · 7-card 2/3/2 staggered grid:**
Row 1: `grid lg:grid-cols-2 gap-6 mb-6` · Row 2: `grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-6` · Row 3: `grid md:grid-cols-2 gap-6` · Footer: gradient takeaways card w/ 2-col sub-cards.

**5. GrowthDriversChallenges · 3-col IconCard w/ nested topics:**
```jsx
<div className="grid md:grid-cols-2 xl:grid-cols-3 gap-6 lg:gap-8">
  <IconCard icon={...} title="Growth Drivers" iconColor="var(--green-600)" showIconBg={false}>
    <div className="space-y-6">
      <div>
        <h4 className="mb-2">Topic</h4>
        <p className="text-[16px] mb-3">...</p>
        <ul className="space-y-1 text-sm">
          <li className="flex items-start gap-2"><Icon className="h-4 w-4 mt-0.5 shrink-0" .../><span>...</span></li>
        </ul>
      </div>
    </div>
  </IconCard>
</div>
```

**6. MarketDataTable · sortable w/ inline progress bar + period pill:**
`<table>` w/ sticky `<caption>` · `<thead>` clickable `<th>` w/ `<CaretUpDown>` · `<tbody>` rows colored by `period` (`bg-white` historical · `bg-[var(--black-50)]` forecast) · cell w/ progress bar = flex gap-3 [`max-w-[120px] bg-[var(--black-200)] rounded-full h-1.5 overflow-hidden`] containing inner `bg-[var(--purple-500)]` w/ inline `width: {pct}%` + side label.

**7. Hero · split video + glass card:**
`grid lg:grid-cols-5 gap-12` · left col-span-3 (badges + h1 + lead + CTA + audio) · right col-span-2 (glass card: h3+accent bar + 2×2 label/value grid + bordered footer w/ product code + glass Button). Video bg behind w/ 2-layer gradient overlay + 0.03-opacity grid pattern + 2 floating blur orbs animated via rAF.

**8. TableOfContentsSidebar:**
sticky `top-[72px] h-[calc(100vh-72px)]` aside · 255px expanded · collapse button `-right-4 bottom-[20%]` half-outside circle · scroll-spy via `useScrollSpy(sectionIds, 200)` · per-item `bg-[#171717] text-white rounded-full size-5` number circle · completed = Check icon · active = `bg-[#f5f5f5] text-[#171717] font-bold`.

**9. ResearchMethodology · stepper + 3-col cards:**
Top: centered stepper `<Button variant="ctaBlack|outline">` chain w/ `<ChevronRight>` separators · per-button leading `size-6 rounded-full bg-white/20|black-200` step number. Below: `grid md:grid-cols-3 gap-4 lg:gap-5` of `<MethodologyCard>` w/ click-to-activate.

**10. FAQ · accordion stack:**
shadcn `<Accordion type="single" collapsible className="space-y-4">` · per `<AccordionItem className="bg-white border border-[var(--black-100)] rounded-[10px] px-6 hover:border-[var(--black-300)] !border-b-[1px]">` · trigger `text-base font-bold` py-5 · content `text-sm leading-relaxed pb-5 text-[var(--black-500)]`.

---

## 9. Layout Patterns

**Page shell:** `<Header>` sticky · `<TableOfContentsSidebar>` sticky-left aside (parallel to main column · lg+) · `<main>` columns of sections · `<FloatingCTA>` fixed bottom · `<FinalCTA>` · `<Footer>`.

**Container:** `max-w-7xl mx-auto` (1280px) + horizontal padding `px-[84.375px] lg:px-[112.5px]` (~84/112px). Inner content sometimes narrowed `max-w-3xl` (768px) for lead paragraphs.

**Grid breakpoints:** `sm: 640` · `md: 768` · `lg: 1024` · `xl: 1280`. Stat grids: `grid-cols-1 sm:grid-cols-2 md:grid-cols-4`. Card grids: `md:grid-cols-2 lg:grid-cols-3` or `md:grid-cols-3` or `xl:grid-cols-3`. Hero: `grid lg:grid-cols-5 gap-12 lg:gap-16`.

**Sticky:** Header `sticky top-0 z-50` · Sidebar `sticky top-[72px] z-40` · scroll-spy via hook · FloatingCTA `fixed bottom-0 inset-x-0 z-50`.

**Bg alternation:** white / `var(--black-50)` (#fafafa) — per chapter: white (MarketOverview · MarketAnalysis · SegmentationSection · GrowthDrivers · Methodology) · black-50 (Scope · MarketDataTable · Regional · Target · FAQ).

**Patterns:** dot-pattern radial-gradient bg overlay on many `--pattern-opacity 0.05` · `--pattern-grid-size 20px`.

---

## 10. Token Gap Report — V0.2 → core-v2

| V0.2 token | Used | core-v2 equivalent | Status |
|---|---|---|---|
| `--brand-red #b01f24` | ✅ | `--brand-red` / `--red-600` | ✅ exact |
| `--purple-500 #7f5fe3` | ✅ | `--purple-500 #9488ec` | 🔴 different hex |
| `--purple-300 #b8aeef` | ✅ | `--purple-300 #c4bef7` | 🔴 different |
| `--purple-100 #eff1fe` | ✅ | `--purple-100 #efedfd` | ⚠️ close |
| `--black-50/100/200/300/500/600/900` | ✅ | aliases of `--color-ramp-black-*` | ✅ exact intent |
| `--black-25 #fcfcfc` | ✅ | none | 🔴 missing |
| `--red-500/600` | ✅ | core-v2 `--red-500 #dc3238` (theme #ef4444) | ⚠️ |
| `--green-600 #16a34a` | ✅ | core-v2 `--green-600 #059669` | 🔴 different |
| `--amber-400 #fbbf24` | ✅ | core-v2 `--amber-400` ✅ | ✅ |
| `--periwinkle-*` | aliased | core-v2 `--periwinkle-*` | ⚠️ different ramp |
| `--glass-bg/border/glow/accent/text/hover` | ✅ Hero | none | 🔴 missing entirely |
| `--shadow-brand-red/-hover/-purple/-periwinkle` | ✅ | core-v2 `--shadow-accent-md` | ⚠️ remap |
| `--radius-md 0.625rem` (10px) | ✅ | core-v2 `--radius-md 15px` | 🔴 NAME COLLISION (close to `--radius-sm 10px` in core-v2) |
| `--radius-sm 0.15625rem` (2.5px) | ✅ | core-v2 `--radius-2xs 2.5px` | 🔴 NAME COLLISION |
| `--radius-lg 1rem` (16px) | ✅ | core-v2 `--radius-md 15px` | ⚠️ |
| `--space-*` numeric | ✅ | core-v2 ✅ aliased | ✅ |
| `--text-sm/base/lg/xl/2xl/3xl/4xl` | ✅ | core-v2 all redefined w/ different rem | 🔴 NAME COLLISION |
| `--font-display` / `--font-sans` | ✅ | core-v2 `--font-serif` / `--font-sans` | ⚠️ rename `display→serif` |
| `--font-bold 700` | ✅ many | core-v2 ships 400 + 500 only | 🔴 OG strips 700 |
| `--leading-tight 1.25` | ✅ | core-v2 `--leading-tight 1.1` | ⚠️ |
| `--leading-relaxed 1.625` | ✅ | core-v2 `--leading-relaxed 1.6` | ✅ ish |
| `--tracking-widest 0.1em` | ✅ | core-v2 ✅ | ✅ |
| `--duration-xs 200ms` | ✅ chart-title-header | core-v2 `--duration-fast 200ms` | ⚠️ rename |
| `--pattern-opacity/grid-size/dot-size/dot-position` | ✅ | none | 🔴 port pattern tokens |
| `--content-max-width` | ref'd MarketOverview:30 | none (undefined in theme.css too) | 🔴 broken ref |

**Hardcoded sins:** `#171717`, `#737373`, `#525252`, `#404040`, `#d4d4d4`, `#e5e5e5`, `#f5f5f5`, `#fafafa`, `#656565`, `#7f5fe3`, `#6D52D9`, `#f4f2fc`, `#eff1fe`, `#e2e4fd`, `#b01f24`, `#B01F24`, `#D72B31`, `#EFEFEF`, `#FFFFFF1A`. All over Header/HeroSection/InlineStats/TimelineCard/StatCard/MindMap/GrowthDrivers/AnalysisCard. Must port to var() refs on migration.

---

## 11. Motion Patterns

**Hero animated orbs (`HeroSection:21-43`):** `requestAnimationFrame` loop incrementing `frame += 0.01` · `Math.sin(frame) * 30` translateX orb1 · `Math.cos(frame) * 30` orb2 · `Math.sin(frame*2) * 3` translateY scroll indicator. **No reduced-motion check** 🔴 a11y violation. Video fade-in `transition-opacity duration-1000`.

**Header scroll-spy bg-flip (`Header:10-29`):** scroll listener flips `isOnLightBackground` past 60vh; smooth `transition-all duration-300` between `bg-black/40 backdrop-blur-lg` and `bg-white shadow-sm`. Progress bar fills `style={{width: ${scrollProgress}%}}` `transition-all duration-150 ease-out`.

**FloatingCTA reveal (`FloatingCTA:46-50`):** `translate-y-full opacity-0` → `translate-y-0 opacity-100` `transition-all duration-500 ease-in-out`. Throttled scroll via rAF tick gate. 🔴 SKIP.

**Sidebar collapse (`TableOfContentsSidebar:65`):** width transition `transition-all duration-300` · ChevronLeft `transition-transform rotate-180`. Per-item `transition-all duration-200`.

**MindMap d3 transitions:** `duration = 800` `d3.easeCubicInOut` on every enter/update/exit · diagonal-curve path interpolation · zoom.scaleExtent([0.1, 4]).

**Card hover shadow:** `hover:shadow-[var(--shadow-brand-purple|periwinkle|red)] transition-shadow duration-300` — IconCard, TextCard, SegmentationCard, MethodologyCard, MarketDataTable wrapper.

**Card hover border:** `hover:border-[var(--black-300|400)]` `transition-colors duration-300` — AnalysisCard, ComparisonParameterCard, FAQ item, TargetAudience aside.

**TextCard expand/accordion (`Accordion`):** shadcn-radix native expand. ✅ keep.

**MindMapModal (`MindMapModal:42-78`):** fixed inset-0 bg-black/60 backdrop-blur-sm · click-outside close · ESC close · body-overflow lock. No entrance animation.

**Chart hover:** Highcharts `states.hover: { brightness: 0.1 }` `RegionalComparison:110` · Chart-title-header legend dot `group-hover:scale-125 duration-[var(--duration-xs)]`.

**Reduced-motion:** ZERO checks across V0.2 components. 🔴 critical gap.

---

## 12. A11y Patterns

**Focus:** No explicit `:focus-visible` rings. Button shadcn default has `focus-visible:ring-[3px]` — limited propagation.

**Keyboard:** Sortable header `<th onClick>` — clickable div, not `<button>`. 🔴 not keyboard-focusable. MindMap d3 click handler — keyboard inaccessible. 🔴. MindMapModal close button `aria-label="Close modal"` ✅. TOC sidebar buttons = proper `<button>` ✅.

**ESC handling:** MindMapModal ✅ ESC + body-lock + click-outside.

**ARIA:** Headings hierarchy: `h1` only hero · `h2` per section · `h3` card titles · `h4` subsection. Mostly correct. SectionHeader allows `as` prop override.

**Landmarks:** `<section id="x">` on every section ✅. `<header>` sticky ✅. `<aside>` sidebar ✅. `<nav>` Header dropdowns ✅. `<main>` — not seen explicitly in components (probably in page.tsx).

**Skip link:** none visible.

**Touch target:** Stepper Button h-12 ✅ (48px). Sidebar collapse 8×8 (32px) 🔴 below 44px. MindMap nodes 36px tall · pill width = text-length × 7.5 + 32 + badge — narrow nodes 🔴.

**Alt text:** Header logo `alt="Ken Research Logo"` ✅. Hero video — no `<track kind="captions">` 🔴.

**Caption table:** `MarketDataTable:75` `<caption>` semantic ✅.

**`accessibility: { enabled: false }`** on every Highcharts options block 🔴 explicit a11y disable.

---

## 13. Cards + Listing Patterns

| Card | File:line | Pattern | Verdict |
|---|---|---|---|
| `analysis-card.tsx:51` | `bg-white border border-[#e5e5e5] rounded-[10px] p-4 hover:border-purple-300` · 40×40 icon container `#f4f2fc bg` w/ `#7f5fe3` text · h4 14px font-bold · p 12px text-[#737373] | ✅ KEEP numbered/icon variant · 🔴 hardcoded hex |
| `methodology-card.tsx:74` | shadcn `<Card>` w/ `gradient-card-purple` class · `p-8` · 44px `rounded-xl bg-[var(--purple-100)]` icon · h3 base bold + p sm · ul space-y-3 w/ ChevronRight bullets · `isActive` ring-2 ring-foreground | ✅ KEEP layout · simplify state |
| `segmentation-card.tsx:87` | `p-4 bg-white border-[var(--black-200)] rounded-[var(--radius-md)] hover:shadow-[var(--shadow-brand-purple)]` · 40×40 icon · h3 lg + p sm · items: flex-between name+share% w/ optional `(cagr)` and description · 2px h `bg-[var(--black-100)]` progress w/ `bg-[var(--purple-300)]` fill animated `duration-700 ease-out` | ✅ KEEP CANONICAL |
| `stakeholder-card.tsx:49` | `flex items-start gap-4 p-5 bg-white border-[var(--black-100)] rounded-[10px]` · 44px `rounded-xl bg-[var(--purple-100)]` icon · h4 base bold + p sm leading-relaxed | ✅ KEEP CANONICAL |
| `stat-card.tsx:99` | 4 variants `icon-left/icon-top/inline/centered` · icon-left: `flex items-start gap-3 bg-warm-200` 🔴 broken class, intended bg-white · `p-4 rounded-[10px] border border-[#f5f5f5]` · purple hover shadow · 40px icon container · label 14px gray + value 18px black bold + subtitle 13px · valueSize sm/default/lg/xl (16/18/22/26px) | ✅ KEEP all 4 variants |
| `stat-card-group.tsx:46` | wrapper: desktop `hidden md:flex items-baseline gap-8` inline-variant + `showDivider` · mobile `grid grid-cols-2 gap-x-8 gap-y-6 md:hidden` centered-variant | ✅ KEEP CANONICAL |
| `text-card.tsx:94` | `border-[var(--black-200)] bg-white rounded-[var(--radius-md)] hover:shadow-[var(--shadow-brand-periwinkle)]` · header `px-6 pt-6 pb-4` w/ optional 40px purple-bg icon + h3 · content `px-6 pb-6` paragraphs (text-base leading-relaxed text-black-600) · optional `stats` grid 2-col separated by `border-t mt-8 pt-8` · stat value `text-[26px] font-bold` (primary→purple else black-900) + label 14px black-600 | ✅ KEEP CANONICAL |
| `timeline-card.tsx:35` | `rounded-[10px] border border-[#f5f5f5] bg-[#fafafa] hover:bg-[#f5f5f5] duration-300` · `p-4 text-center` · label 14px black-500 + value 26px black-900 bold | ✅ KEEP CANONICAL · 🔴 hardcoded hex |
| `icon-card.tsx:66` | `h-full p-4 bg-white border-[var(--black-200)] rounded-[var(--radius-md)] hover:shadow-[var(--shadow-brand-purple)]` · icon `mb-4 rounded-lg` w/ purple-100 bg toggle · `iconSize sm/md/lg` 32/40/48 · h3 mb-3 · description 16px text-black-500 OR custom children | ✅ KEEP CANONICAL |
| `comparison-parameter-card.tsx:53` | `h-full p-4 bg-white border-[var(--black-200)] rounded-[var(--radius-md)] hover:border-[var(--black-300)]` · 40px purple-100 icon OR `text-base font-bold purple-500` number · h4 sm bold + p xs leading-relaxed text-black-500 | ✅ KEEP CANONICAL |
| `RelatedReports.tsx` listing | Card grid (251L · not full-read) | ✅ KEEP grid · 🔴 SKIP card-CTA |

**Card token use sins:** `bg-warm-200` on `stat-card.tsx:151` is undefined class (intended bg-white) → broken. Several cards mix var() and hex hardcoded.

---

**END OF AUDIT** — Word count ~2480.
