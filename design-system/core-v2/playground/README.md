# Playground

Vite SPA for local component development. NOT included in package exports.

```sh
# From core-v2/ root
pnpm playground
# Opens at http://localhost:5175
```

Develop atoms here before writing Storybook stories. Route per atom added in Phase C.

For visual regression + docs: use `pnpm storybook` instead.
